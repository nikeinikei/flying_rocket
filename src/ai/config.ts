export const config = {
    // the model takes time to process the input data
    // also there are delays in transmitting
    // that's why only fraction of all physics updates actually get sent to the ai
    frameQuotient: 3
};
